import 'package:flutter/material.dart';

class BatmanBackground extends StatelessWidget {
  const BatmanBackground({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/batman_background.png');
  }
}
