import 'package:flutter/material.dart';

class BatmanAlone extends StatelessWidget {
  const BatmanAlone({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/batman_alone.png');
  }
}
